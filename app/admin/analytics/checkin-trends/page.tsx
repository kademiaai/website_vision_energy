import { TrendingUp } from "lucide-react";

export default function CheckinTrendsPage() {
  return (
    <div className="admin-card py-16 text-center">
      <TrendingUp className="mx-auto mb-3 text-muted-foreground" size={32} />
      <h2 className="text-lg font-semibold text-foreground">Xu hướng check-in</h2>
      <p className="text-muted-foreground text-sm mt-1">Đang được xây dựng.</p>
    </div>
  );
}
